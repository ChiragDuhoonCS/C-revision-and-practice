#include<stdio.h>

int main(){
    int a;
    // scanf to get input from user
    scanf("%d", &a);
    // & means adress of
    // %d means placeholder of user input 
    /* %f %c for float for character respectively 
    */
    printf("the value of this is %d", a);
    return 0 ;
}